"use strict";

/*
* PROBLEM:
* DESIGN a function called area that consumes the length of one side 
* of a square and produces the area of the square.
* Remember, when we say DESIGN, we mean follow the recipe.
* Leave behind commented out versions of the stub and template.
*/

// Number -> Number
// produces the area of the square by multiplying the input number with itself

// stub
// function area (inputLength) {
//     return 0;
// }

// template
// function area (inputLength) {
//     return (... inputLength);
// }

function area (inputLength) {
    return (inputLength * inputLength);
}
